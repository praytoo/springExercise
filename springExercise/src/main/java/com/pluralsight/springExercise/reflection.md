Why is it crashing?
-because the bean that is meant to be consumed must be specified using @Primary or @Qualifier

You can fix this with the @Qualifier annotation, do independent research to solve this
-by adding @Primary or @Qualifier we tell Spring which bean to consume.